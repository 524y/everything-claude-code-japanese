# Python セキュリティ

> This file extends [common/security.md](../common/security.md) with Python specific content.

## シークレット管理

`os.environ` で環境変数を読み、未設定時は失敗させる。

## セキュリティスキャン

- **bandit** を使う

## Reference

Django を使う場合は `django-security` skill を参照。
