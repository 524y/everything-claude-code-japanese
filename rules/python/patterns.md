# Python パターン

> This file extends [common/patterns.md](../common/patterns.md) with Python specific content.

## Protocol（Duck Typing）

`typing.Protocol` で契約を定義する。

## Dataclasses as DTOs

`@dataclass` で DTO を表現する。

## Context Managers & Generators

- `with` でリソース管理
- generator で遅延評価と省メモリ化

## Reference

`python-patterns` skill を参照。
