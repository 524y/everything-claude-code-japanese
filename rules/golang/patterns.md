# Go パターン

> This file extends [common/patterns.md](../common/patterns.md) with Go specific content.

## Functional Options

`Option func(*Server)` で柔軟な初期化を行う。

## Small Interfaces

interface は実装側ではなく利用側で定義する。

## Dependency Injection

constructor 関数で依存注入する。

## Reference

`golang-patterns` skill を参照。
